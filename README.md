# directadmin_ssl_validation_checker
Gets SSL for all domains that point to server with no valid cert 

```bash
curl -fsSL https://raw.githubusercontent.com/ShahaB108/directadmin_ssl_validation_checker/main/sslchecker.sh | bash
```
